{
  "name": "escrs-app",
  "version": "1.0.0",
  "description": "Calculateur IOL ESCRS — interface desktop",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && tsc -p src/renderer/tsconfig.json",
    "start": "npm run build && electron .",
    "dev":   "tsc --watch & tsc -p src/renderer/tsconfig.json --watch & electron . --dev"
  },
  "dependencies": {
    "electron": "^30.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
